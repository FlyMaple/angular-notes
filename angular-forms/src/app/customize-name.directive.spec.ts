import { CustomizeNameDirective } from './customize-name.directive';

describe('CustomizeNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizeNameDirective();
    expect(directive).toBeTruthy();
  });
});
